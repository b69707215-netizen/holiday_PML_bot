# 🤖 Бот для автоматизации школы

Telegram-бот для автоматизации процессов формирования приказов, учета отпускных дней сотрудников и коммуникации между учителями и секретарем.

## 📋 Возможности

### 👨‍🏫 Для учителей:
- ✅ Регистрация с указанием ФИО и телефона
- 📊 Просмотр оставшихся дней отпуска
- 📝 Подача заявок на отпуск
- 📄 Автоматическое получение приказов после одобрения

### 👩‍💼 Для секретаря:
- 📨 Уведомления о новых заявках на отпуск
- ✅/❌ Одобрение или отклонение заявок
- 📄 Автоматическая генерация приказов (Word .docx)
- 👥 Просмотр списка сотрудников
- 📊 Мониторинг отпусков
- 📄 Создание приказов по шаблонам

## 🚀 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd "ТГ бота для автоматизацый школы"
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл .env:**
```bash
cp .env.example .env
```

5. **Настройте переменные окружения в .env:**
```
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=sqlite:///school_bot.db
SECRETARY_TELEGRAM_ID=123456789
```

- `BOT_TOKEN` — получите у [@BotFather](https://t.me/BotFather)
- `SECRETARY_TEGRAM_ID` — Telegram ID секретаря (опционально, для уведомлений)

## 🎮 Запуск (локально)

```bash
python main.py
```

## 🚀 Деплой на Railway

Бот готов к деплою на [Railway](https://railway.com). Следуйте инструкциям:

### 1. Подготовка

Убедитесь, что проект запушен на GitHub:
```bash
git push origin master
```

### 2. Создание проекта на Railway

1. Перейдите на [Railway Dashboard](https://railway.com/dashboard)
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите репозиторий `holiday_PML_bot`
4. Railway автоматически обнаружит `railway.toml` и `Procfile`

### 3. Настройка переменных окружения

В настройках проекта на Railway добавьте переменные:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `BOT_TOKEN` | `8929817821:AAGsy-296jmNKWVcFt0CCRRINcRL1HluOy4` | Токен бота |
| `DATABASE_URL` | `sqlite:///data/school_bot.db` | Путь к БД (сохраняется между деплоями) |
| `SECRETARY_TELEGRAM_ID` | `123456789` | ID секретаря для уведомлений |

### 4. Добавление Volume для сохранения данных

Чтобы база данных не терялась при перезапуске:

1. В Railway Dashboard перейдите в **Settings** проекта
2. Добавьте **Volume** с mount path: `/app/data`
3. Измените `DATABASE_URL` на `sqlite:///data/school_bot.db`

### 5. Деплой

Railway автоматически деплоит бот при каждом пуше в `master`. 

Для ручного деплоя: **Deploy → Deploy Now**

### 📊 Мониторинг

- Логи: **Deployments → Logs**
- Метрики: **Metrics** (CPU, Memory)
- Перезапуск: **Restart** в случае ошибок

## 📁 Структура проекта

```
.
├── config.py              # Конфигурация бота
├── main.py                # Точка входа
├── states.py              # FSM состояния
├── keyboards.py           # Клавиатуры
├── requirements.txt       # Зависимости
├── .env.example           # Пример переменных окружения
├── railway.toml           # Конфигурация деплоя на Railway
├── Procfile               # Команда запуска для Railway
├── database/
│   ├── __init__.py
│   ├── models.py          # SQLAlchemy модели
│   └── db.py              # Настройка БД
├── handlers/
│   ├── __init__.py
│   ├── common.py          # Общие обработчики
│   ├── teacher.py         # Обработчики для учителей
│   └── secretary.py       # Обработчики для секретаря
├── services/
│   ├── __init__.py
│   └── doc_generator.py   # Генерация документов
└── 2025-20260608T221020Z-3-001/
    └── 2025/              # Шаблоны приказов (.docx)
```

## 📄 Шаблоны приказов

Шаблоны документов находятся в папке `2025-20260608T221020Z-3-001/2025/`:
- 🌸 Весняні канікули
- 💪 Молодь за здоров'я
- 🛡️ 16 днів проти насильства
- 🏕️ Джура

## 🛠 Технологии

- **aiogram 3.x** — фреймворк для Telegram Bot API
- **SQLAlchemy 2.x** — ORM для работы с БД
- **aiosqlite** — асинхронный драйвер SQLite
- **python-docx** — работа с Word документами
- **python-dotenv** — управление переменными окружения

## 📊 База данных

### Таблицы:
- **users** — сотрудники (учителя и секретарь)
- **vacations** — заявки на отпуск
- **orders** — история созданных приказов

## 🔐 Безопасность

- Храните `BOT_TOKEN` в файле `.env` и не добавляйте его в Git
- Ограничьте доступ к файлам с конфиденциальной информацией
- Регулярно делайте резервные копии базы данных

## 📝 Лицензия

MIT License
